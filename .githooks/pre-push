#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(git rev-parse --show-toplevel)"
cd "$ROOT_DIR"

echo "Pre-push: uruchamiam predeploy gate + build/export..."
node scripts/run-all-prepush.js
echo "Pre-push: OK."
