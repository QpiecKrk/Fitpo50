---
name: shell
description: "Shell for Fitpo50. 1 conventions, 2 fixes."
domain: shell
triggers:
  - glob: "**/*.sh"
  - glob: "**/*.bash"
autoGenerated: true
enabled: true
---

# Shell

Auto-compiled from **10 real patterns** in **Fitpo50**. This skill is auto-routed to agents when working on shell files.

## 🔧 Problem Playbooks

### problem-fix in export_site.sh
File updated (external): scripts/export_site.sh

Content summary (65 lines):
#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
OUTPUT_DIR="${1:-$ROOT_DIR/_site}"

run_ts_build() {
  if [[ "${SKIP_TS_BUILD:-0}" == "1" ]]; then
    echo "SKIP_TS_BUILD=1 -> pomijam build TypeScript."
    return 0
  fi

  if ! command -v npm >/dev/null 2>&1; then
    echo "ERROR: npm

**Actionable Steps:**
1. Modified 1 files

### problem-fix in export_site.sh
File updated (external): scripts/export_site.sh

Content summary (65 lines):
#!/usr/bin/env bash

set -euo pipefail

ROOT_DIR="$(cd "$(dirname "$0")/.." && pwd)"
OUTPUT_DIR="${1:-$ROOT_DIR/_site}"

run_ts_build() {
  if [[ "${SKIP_TS_BUILD:-0}" == "1" ]]; then
    echo "SKIP_TS_BUILD=1 -> pomijam build TypeScript."
    return 0
  fi

  if ! command -v npm >/dev/null 2>&1; then
    echo "ERROR: npm

**Actionable Steps:**
1. Modified 1 files

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **what-changed in copy_pngs.sh — confirmed 3x** — File updated (external): _site/copy_pngs.sh

Content summary (15 lines):
#!/bin/bash
ASSETS_DIR="/Us

---
*Auto-generated by BrainSync 🧠 | 10 patterns | 2026-03-30*
