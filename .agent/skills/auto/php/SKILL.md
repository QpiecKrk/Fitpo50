---
name: php
description: "Php for Fitpo50. 5 conventions, 6 fixes."
domain: php
triggers:
  - glob: "**/*.php"
autoGenerated: true
enabled: true
---

# Php

Auto-compiled from **38 real patterns** in **Fitpo50**. This skill is auto-routed to agents when working on php files.

## 🔧 Problem Playbooks

### Fixed null crash in Wpisy — prevents null/undefined runtime crashes
- $dateFormatted = date('j F Y', strtotime($date ?? ''));
+ $uploadsUrl = $siteUrl . 'admin/uploads/';
- $pageTitle = 'Wpisy z ' . $dateFormatted;
+ $dateFormatted = date('j F Y', strtotime($date ?? ''));
- $pageDesc  = 'Wszystkie wpisy FitPo50 z dnia ' . $dateFormatted . '.';
+ $pageTitle = 'Wpisy z ' . $dateFormatted;
- $pageUrl   = $siteUrl . 'sukcesy/' . ($date ?? '') . '.html';
+ $pageDesc  =

**Actionable Steps:**
1. Modified 1 files
2. identifier: Wpisy
3. identifier: Wszystkie
4. identifier: Domy
5. identifier: Wybierzmy

### Fixed null crash in YYYY — prevents null/undefined runtime crashes
- $slug       = $entry['slug'];
+ $uploadsUrl = $siteUrl . 'admin/uploads/';
- $title      = $entry['title'];
+ $slug       = $entry['slug'];
- $lead       = $entry['lead'] ?? '';
+ $title      = $entry['title'];
- $content    = $entry['content'];
+ $lead       = $entry['lead'] ?? '';
- $date       = $entry['entry_date'];   // YYYY-MM-DD
+ $content    = $entry['content'];
- $dateIso    = $date . '

**Actionable Steps:**
1. Modified 1 files
2. identifier: YYYY
3. identifier: Pierwszy
4. identifier: Czas
5. identifier: Helper

### Patched security issue POST — prevents null/undefined runtime crashes
-         <?php if (empty($errors)): ?>
+         <form method="POST" action="sync-manual.php" style="margin-top: 20px;">
-             <p class="success">✓ Synchronizacja zakończona pomyślnie.</p>
+             <input type="hidden" name="csrf_token" value="<?= h($_SESSION['csrf_token'] ?? '') ?>">
-         <?php endif; ?>
+             <button type="submit" class="btn" style="background: #27ae60

**Actionable Steps:**
1. Modified 1 files
2. identifier: POST
3. identifier: Uruchom
4. identifier: Powr
5. identifier: Synchronizacja

### Fixed null crash in Wpis — prevents XSS injection attacks
-     foreach (array_unique($affectedDates) as $d) {
+     $syncedCount = 0;
-         syncDay($db, $d);
+     foreach (array_unique($affectedDates) as $d) {
-     }
+         $syncedCount = syncDay($db, $d);
- 
+     }
-     $_SESSION['flash_success'] = $status === 'published'
+ 
-         ? 'Wpis opublikowany! Strona dnia i kalendarz zaktualizowane.'
+     $_SESSION['flash_success'] = $status ==

**Actionable Steps:**
1. Modified 1 files
2. identifier: Wpis
3. identifier: Kalendarz
4. identifier: Location
5. identifier: Exception

### problem-fix in sync-manual.php
File updated (external): admin/sync-manual.php

Content summary (78 lines):
<?php
require_once __DIR__ . '/config.php';
require_once __DIR__ . '/auth.php';
require_once __DIR__ . '/helpers/calendar.php';

// Wymagamy zalogowania, aby nie każdy mógł wywołać sync
requireLogin();

$db = getDb();
$errors = [];
$stats = [];

try {
    // 1. Sprawdź liczbę wpisów w bazie
    $stmt = $db->query("SELECT C

**Actionable Steps:**
1. Modified 1 files

### Fixed null crash in Nadpisz — prevents null/undefined runtime crashes
-     $content = preg_replace('/\s*\{[^}]*date:\s*"' . preg_quote($date, '/') . '"[^}]*\},?/', '', $content);
+     if ($content === false) return;
-     if ($content !== null) file_put_contents($calFile, $content);
+ 
- }
+     [$entries, $found] = readCalendarEntries($content);
- 
+     if (!$found) return;
- function injectCalendarEntry(string $date, string $url): void {
+ 
-     $calFile = SIT

**Actionable Steps:**
1. Modified 1 files
2. identifier: Nadpisz
3. identifier: SITE_ROOT
4. identifier: ENTRIES_START
5. identifier: ENTRIES_END

## 📐 Conventions & Best Practices

### Project Conventions
- 📐 **Fixed null crash in DOMDocument — prevents null/undefined runtime crashes — confirmed 3x** — -     $allowed = ['image/jpeg','image/png','image/webp','image/gif'];
+     $allowed = ['image/jpeg'
- 📐 **Added session cookies authentication — prevents null/undefined runtime crashes — confirmed 3x** — -         $synced = true;
+         sitemapRebuild($db);
-         $stats[] = "<span class='success'
- 📐 **Replaced auth Panel — prevents null/undefined runtime crashes — confirmed 3x** — - $logoUrl = 'assets/logo.jpg';
+ $logoUrl = 'assets/logo.jpg?v=2';
-       <img src="<?= h($logoUrl
- 📐 **Updated entries database schema — confirmed 4x** — - function syncDay(PDO $db, string $date): void {
+ function syncDay(PDO $db, string $date): int {
-
- 📐 **Strengthened types Nawigacja — prevents null/undefined runtime crashes** — - .article-hero { position: relative; width: 100%; max-width: 1000px; margin: 0 auto var(--space-16)

## 🔵 Architecture

### how-it-works in day-list.php
- .article-header { text-align: center; margin-bottom: var(--space-6); padding: 0 var(--space-6); max-width: 980px; margin-inline: auto; }
+ .article-header { text-align: center; margin-bottom: var(--

### how-it-works in day-list.php
- .article-header { text-align: center; margin-bottom: var(--space-8); padding: 0 var(--space-6); max-width: 980px; margin-inline: auto; }
+ .article-header { text-align: center; margin-bottom: var(--

## 🤔 Decisions & Trade-offs

- **trade-off in article.php** — - <script>
+ <?php require ADMIN_ROOT . 'templates/carousel-script.php'; ?>
- document.addEventListe

---
*Auto-generated by BrainSync 🧠 | 38 patterns | 2026-03-29*
